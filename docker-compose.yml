services:
  server:
    build: .
    ports:
      - "8099:8080"
    environment:
      - PORT=8080
      - DB_PATH=/data/swansea.db
    env_file:
      - .env
    volumes:
      - ./db:/data
    restart: unless-stopped
